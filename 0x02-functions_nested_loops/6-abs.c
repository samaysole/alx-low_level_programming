#include "main.h"

/**
 * _abs - absolute value integer calculator
 * @i : integer value
 * Return: Absolute value
 */

int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
